server:
  port: 8095

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Heebum/microservicios.git
          searchPath: config-data
          username: ${GIT_USER}
          PASSWORD: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t